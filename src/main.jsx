import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Ensure this is pointing to the right CSS file
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
